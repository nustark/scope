# Creating a custom knowledge base with LangChain, Pinecone, and GPT-3
* Load documents (pdfs)
* Split text into chunks
* Generate embeddings for text
* Index documents using pinecone
* Use retrieval QA to query GPT model